# Production — used by `npm run build`
# In production nginx proxies /api → backend, so the path stays relative.
VITE_API_BASE_URL=/api/v1
VITE_APP_TITLE=ArticNexus

# External product URLs (adjust to real domains)
VITE_VETDATA_URL=https://vetdata.articdev.com
VITE_OFTADATA_URL=https://oftadata.articdev.com
