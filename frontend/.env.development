# Development — used by `npm run dev`
# The Vite proxy in vite.config.js forwards /api → http://localhost:8080
VITE_API_BASE_URL=/api/v1
VITE_APP_TITLE=ArticNexus (Dev)

# External product URLs (for landing page links)
VITE_VETDATA_URL=http://localhost:5174
VITE_OFTADATA_URL=http://localhost:5173
